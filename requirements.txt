numpy>=1.24
pandas>=2.0
scipy>=1.10
torch>=2.0
wfdb>=4.1
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
ipykernel>=6.25
scikit-learn>=1.3
statsmodels>=0.14

# Optional: required only for diagnostic-level (L3) evaluation with the
# Ribeiro et al. (2020) classifier on CODE-test
tensorflow-cpu>=2.13; sys_platform != 'darwin'
tensorflow>=2.13; sys_platform == 'darwin'
